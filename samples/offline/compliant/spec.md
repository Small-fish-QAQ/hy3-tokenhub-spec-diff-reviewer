# Greeting formatter

Implement `greet(name)` in `src/greet.js`.

1. Trim leading and trailing whitespace from the supplied name.
2. Return `Hello, stranger!` when the trimmed name is empty.
3. Return `Hello, <name>!` for a non-empty name.
4. Add tests for the empty and non-empty cases.
