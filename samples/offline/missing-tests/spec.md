# Slug normalizer

Implement `slugify(title)`.

1. Lowercase the title and replace whitespace with one hyphen.
2. Remove leading and trailing hyphens.
3. Add tests for repeated whitespace and empty input.
